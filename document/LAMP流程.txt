

 1，脚本目前是为64位系统写的，暂时不支持32位系统；
        2，请留意的/tmp/.config.list文件，这个文件保存了MySQL root用户密码以及网站域名及该域名对应的数据库名、数据库用户和密码。
            这个文件中内容是由python程序生成供shell脚本使用的。当shell脚本使用完成后，python程序需要将数据库root密码存放到其他位置（密码以后有可能会用到，位置自定义即可）。然后将.config.list文件中的其余信息（网站域名及对应的数据库名等）存放到项目的web-site文件中。然后将.config.list文件清空。
      3，我们在执行安装过程的时候，原则上是尽量不去更改用户原来存放的数据。比如MySQL的安装，如果用户之前安装了MySQL，那么我们把之前的MySQL服务禁用即可，不删除用户原来已有的数据。
      4， 脚本目录已经更新，脚本程序中需要的安装包都会放在package下，所以python程序在从百度云存储（BCS）下载软件包时，将下载后的文件放到package中（文件名不需更改）。




1、检查用户的服务器的操作系统的类型
2、检查用户使用安装了apache，php，mysql
3、如果用户安装了


自动分区

