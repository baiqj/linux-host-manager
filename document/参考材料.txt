谷歌宣布正在开发一种新的网络协议“SPDY”(发音同“speedy”)，以最小化网络延迟，提升网络速度，优化用户的网络使用体验。

SPDY并不是一种用于替代HTTP的协议，而是对HTTP协议的增强。新协议的功能包括数据流的多路复用、请求优先级，以及HTTP包头压缩。谷歌已经开发一个网络服务器原型机，以及支持SPDY协议的Chrome浏览器版本。

谷歌表示，引入SPDY协议后，在实验室测试中页面加载速度比原先快64%。这一数据基于对全球25大网站的下载测试。目前SPDY团队已经开发了一个可使用的原型产品，谷歌决定开放这一项目，希望“网络社区能积极参与、提供反馈及帮助”。



DoS攻击是一个永恒的问题，虽然专业厂商的防火墙，负载均衡类的网关设备能比较有效的防御DoS攻击，但黑客们更倾向于x86+GNU/Linux的组合，原因很简单：足够的廉价。在linux内核3.13里终于加入了SYNPROXY的新功能，这个模块是一个基于链接跟踪的netfilter扩展，主要干的工作就是把来自客户端的初始SYN包标记成UNTRACKED然后直接导入iptables的"SYNPROXY"的动作(类似ACCEPT,NFQUEUE和DROP)，这时内核会扮演网关设备的角色继续跟客户端进行TCP的常规握手流程，SYNPROXY会等到最终的ACK(三次握手)的cookie被验证合法后才会开始让包真正的进入目标端，开发者Jesper Dangaard Brouer的数据表明SYNPROXY对于对抗SYN FLOOD DOS攻击是非常有效的，笔者今天也在Debian和SLES-12-beta2对SYNPROXY进行了DoS测试，大致结果是在使用hping3和metasploit进行测试，开启SYNPROXY后ksoftirq占用会从8%降低到3%以内，有兴趣的solidoter可以亲自去玩玩。"

mod_spdy 是用于 Apache HTTP 服务器的 SPDY 协议实现模块。

apache2nginx (A2N) 是一个开源的Linux下运行的命令行工具，能将Apache的配置文件转换成相应的Nginx的配置文件。本工具能自动将Apache Web服务器的配置文件转化成Nginx的配置文件，减少WEB服务器迁移的工作量。　该工具从Apache Web服务器中的各模块功能的角度进行分析，生成Nginx中相应模块的配置方式，然后以报告的形式生成转换后的Nginx配置文件。

mod_python是apache组织的一个项目，通过它，可以开发psp或cgi，mod_python功能强大，速度快，是非常优秀的web开发工具

如果忘记MySQL root密码，如何重设密码？
执行如下命令：wget http://soft.vpser.net/lnmp/ext/reset_mysql_root_password.sh;sh reset_mysql_root_password.sh 按提示操作即可



黑链检测：黑链检查器吧，这个可以检查出0像素、1像素、背景同色、隐藏层、负坐标、JS隐藏、遮挡等8大类主流黑链代码

第一 增加屏蔽国外IP访问  第二 增加安全狗服务器版 日志可以显示哪个网站被CC

2.建议软件增加类似百度云和360网盘的秒传功能，通过比较本地文件大小 MD5 SHA256值来确定服务器端是否存在该文件，如果存在，无需重新上传，可以方便站长备份服务器环境 如MYSQL等安装包，方便更换服务器活重做系统后的恢复。

3.建议增加上传后的MD5校验比对功能，便于确定服务器端备份的文件和客户端备份的文件是一模一样的，避免由于备份后重装系统，下载后发现文件损坏或者缺失造成的损失，在使用金山快盘的过程中，发生过文件成功上传，但是下载下来无法使用的情况（大小不超过100M，未超过金山限制）
4.数据库备份功能中建议增加数据库增量备份，因为我观察，整个数据库的备份过程是先做全库备份，后进行压缩，最后以文件模式进行上传，这种操作模式下，完全可以开发出增量备份。以降低数据库的数据传送量，对于小站来说，增量和完全备份的文件大小可能不大，但对于几个G的数据库的大站来说，增量备份可以节省很多备份空间和网络带宽。


1.安全狗网站和服务器安全狗很好用，希望云备份也增加以下功能
备份全部数据库时只能选一个数据库，但常用常用服务器都装几十个数据库。手工去一个个备份很麻烦。
建议增加可全选或部分选择数据库功能，数据库名称mysql可以用show databases;来获取。sql server 系列数据库可以用
SELECT name FROM master.dbo.sysdatabases
WHERE name NOT IN ('master','model','msdb','tempdb','distrbution') 来获取，通过循环即可备份所有数据库。

2.建议对整个云备份中心的架构进行可研并进行相关优化，通过双主 集群 热备等高可用技术实现在升级系统时广大站长可以恢复备份。
备份可以延期再进行，但是恢复备份的话，往往是网站发生了相关问题，如果不能及时恢复，会被降权。
而且互联网行业里，升级要停止业务，也显得不是很专业。
具体可以参考下ORACLE RAC的滚动升级的设计理念，不一定要用ORACLE，但是理念可以借鉴的。

3.可否考虑对接百度云等云盘。正面影响：a.安全狗公司本身不需要再耗费资金采购硬件存储海量备份
b.鉴于百度等大网盘提供商在全国各地的服务器分布，可以提供较好的上传及下载速度，节省安全狗本部服务器带宽。
负面影响：c. 对接后上传到百度的数据需要进行校验，避免发生备份无效的情况。毕竟采用第三方网盘不好控制。
另一方面就是对接后安全狗无法通过收费提供更多的空间，但可以考虑分2种套餐一种是安全狗公司的存储空间。比如10元10G每月
另一种就是 对接百度的接口收费，对功能收费，30元每月，可以对接百度云。让站长自行选择。
西部数据的wd book live已经实现对接百度云，自动同步NAS和百度云的内容，较受市场欢迎。

4.建议数据库还原前进行备份提示，备份当前数据，以防止忘记或者误点造成数据库恢复后丢失现在的数据。


